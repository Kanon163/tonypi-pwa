# BLOCKERS

## 当前阻塞

- 无阻塞，`tasks/0007` 可交付审查。

## 待主控/集成确认

- 是否接受 `wave2_demo_default` 为第二关产品默认节奏。
- `RobotCommand` 是否需要标准化 `speech` 与 `action_group` 的资源 ID 映射字段。
- `SessionEvent.payloadFields` 当前是字段声明，不是 JSON Schema；若 PWA 需要强校验，Wave 2 需补 schema。
- `BGM03` 不属于本关必需资源，本任务未处理。
